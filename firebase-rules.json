{
  "rules": {
    ".read": false,
    ".write": false,
    "templates": {
      ".read": true,
      "$id": {
        ".write": "auth != null && auth.uid === \"i3J3kuqMLtUglNE1EyC7JZCzaFi1\"",
        "totalUses": {
          ".write": true,
          ".validate": "(!data.exists() && newData.isNumber() && newData.val() == 1) || (data.isNumber() && newData.isNumber() && newData.val() == data.val() + 1)"
        },
        "useHistory": {
          "$event": {
            ".write": true,
            ".validate": "newData.val() === true"
          }
        },
        "usesByDay": {
          "$day": {
            ".write": true,
            ".validate": "(!data.exists() && newData.isNumber() && newData.val() == 1) || (data.isNumber() && newData.isNumber() && newData.val() == data.val() + 1)"
          }
        }
      }
    },
    "prompts": {
      ".read": true,
      "$id": {
        ".write": "auth != null && auth.uid === \"i3J3kuqMLtUglNE1EyC7JZCzaFi1\"",
        "totalCopies": {
          ".write": true,
          ".validate": "(!data.exists() && newData.isNumber() && newData.val() == 1) || (data.isNumber() && newData.isNumber() && newData.val() == data.val() + 1)"
        },
        "copyHistory": {
          "$event": {
            ".write": true,
            ".validate": "newData.val() === true"
          }
        },
        "copiesByDay": {
          "$day": {
            ".write": true,
            ".validate": "(!data.exists() && newData.isNumber() && newData.val() == 1) || (data.isNumber() && newData.isNumber() && newData.val() == data.val() + 1)"
          }
        }
      }
    }
  }
}
